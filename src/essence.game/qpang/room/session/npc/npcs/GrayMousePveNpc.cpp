#include "GrayMousePveNpc.h"

GrayMousePveNpc::GrayMousePveNpc(const eNpcType type, const Position position, const uint16_t baseHealth, const uint16_t initialSpawnRotation)
	: PveNpc(type, position, baseHealth, initialSpawnRotation)
{
}
